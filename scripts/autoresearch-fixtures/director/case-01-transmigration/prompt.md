你是 director agent。读取下方的 script.json，执行以下操作：

1. 为每个 actor 追加 visual 字段（外观描述，供图片生成）
2. 为每个 location 追加 visual 字段
3. 为每个 scene 追加 shots 数组（按 storyboard skill 规则，每 shot ≤15s，含 PART/Beats/切镜头）

请按 storyboard skill 的硬规则生成分镜，输出完整的 enriched script.json。
