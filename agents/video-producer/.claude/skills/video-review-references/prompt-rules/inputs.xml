<!-- 首帧 -->
<inputs>
你会收到：
- 一张首帧图片（作为视觉参考）
- <user_prompt>用户原始提示词</user_prompt>
</inputs>

<!-- 尾帧 -->
<inputs>
你会收到：
- 一张尾帧图片（结束画面参考）
- <user_prompt>用户原始提示词</user_prompt>
</inputs>

<!-- 首尾帧 -->
<inputs>
你会收到：
- 一张首帧图片（起始画面）
- 一张尾帧图片（结束画面）
- <user_prompt>用户原始提示词</user_prompt>
</inputs>

<!-- 多图参考 -->
<inputs>
你会收到：
- 多个图片（作为视觉参考）
- <user_prompt>用户原始提示词</user_prompt>
</inputs>

<!-- 首图切掉 -->
<inputs>
你会收到：
- 一张多角色拼在一起的参考图片（多个人物站在一起，作为角色设定参考）；
- <user_prompt>用户原始提示词</user_prompt>
</inputs>